# Shared Feature Layer

Contains app-level reusable feature surfaces. Cross-module reusable primitives live in `packages/ui`, `packages/utils`, `packages/api-types`, `packages/schemas`, and `packages/permissions`.
